# Xcode (project is generated by xcodegen)
*.xcodeproj/
xcuserdata/
DerivedData/
*.xcresult

# SwiftPM
.build/
.swiftpm/
Package.resolved
# Where CI's xcodebuild clones package checkouts (-clonedSourcePackagesDirPath);
# running the same command locally leaves it in the tree.
SourcePackages/

# macOS
.DS_Store

# Fetched by Scripts/fetch-models.sh, not committed (~93 MB)
Cheerio/Resources/Models/
